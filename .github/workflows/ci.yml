name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ─── Lint ─────────────────────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Replace the steps below with commands appropriate for your stack.
      # Examples:
      #   Node/TS:  npx eslint . && npx tsc --noEmit
      #   Python:   pip install ruff && ruff check .
      #   Rust:     cargo clippy -- -D warnings
      #   Go:       golangci-lint run

      - name: Placeholder — configure for your stack
        run: echo "Add lint commands here"

  # ─── Test ─────────────────────────────────────────────────────────────────
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Replace with real test commands.
      # Examples:
      #   Node:    npm ci && npm test
      #   Python:  pip install -r requirements.txt && pytest
      #   Rust:    cargo test
      #   Go:      go test ./...

      - name: Placeholder — configure for your stack
        run: echo "Add test commands here"

  # ─── Build ────────────────────────────────────────────────────────────────
  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4

      # TODO: Replace with real build commands.
      # Examples:
      #   Node:   npm ci && npm run build
      #   Python: python -m build
      #   Rust:   cargo build --release
      #   Docker: docker build .

      - name: Placeholder — configure for your stack
        run: echo "Add build commands here"

  # ─── Security audit ───────────────────────────────────────────────────────
  audit:
    name: Security audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Replace with dependency audit command.
      # Examples:
      #   Node:   npm audit --audit-level=high
      #   Python: pip install pip-audit && pip-audit
      #   Rust:   cargo audit
      #   Go:     govulncheck ./...

      - name: Placeholder — configure for your stack
        run: echo "Add security audit command here"
